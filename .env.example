# Port used to open Navichrome in your browser. The variable name is retained
# for compatibility with existing deployments.
MONOCHROME_PORT=3002

# Address of Navidrome as seen from the Navichrome container.
# This default works when Navidrome publishes port 4533 on the Docker host.
NAVIDROME_URL=http://host.docker.internal:4533
